import 'package:flutter/material.dart';

class ProductSpecificationsPage extends StatelessWidget {
  const ProductSpecificationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('محتوى صفحة مواصفات المنتج')),
    );
  }
}
